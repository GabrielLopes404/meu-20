# Backend API URL (usado pelo frontend)
# Em produção, deve ser a URL do seu backend no Render
# Exemplo: https://seu-app.onrender.com
VITE_API_URL=http://localhost:5000

# Node Environment
NODE_ENV=development

# Server Port (usado apenas em desenvolvimento local)
PORT=5000

# Database (Opcional - se precisar de banco de dados)
# DATABASE_URL=postgresql://user:password@host:port/database
